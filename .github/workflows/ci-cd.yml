name: CI/CD Pipeline

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: Start SQL Server
      run: |
        docker run -d --name sql-server \
          -e ACCEPT_EULA=Y \
          -e SA_PASSWORD=YourStrong@Pass \
          -e MSSQL_PID=Express \
          -p 1433:1433 \
          mcr.microsoft.com/mssql/server:2022-latest
        sleep 20
    
    - name: Run tests
      run: |
        dotnet test Gamesbakery.BusinessLogic.Tests/Gamesbakery.BusinessLogic.Tests.csproj
        dotnet test Gamesbakery.DataAccess.Tests/Gamesbakery.DataAccess.Tests.csproj
        dotnet test Gamesbakery.E2E.Tests/Gamesbakery.E2E.Tests.csproj
      env:
        ConnectionStrings__DefaultConnection: "Server=localhost;Database=master;User Id=SA;Password=YourStrong@Pass;TrustServerCertificate=true;"
        
    - name: Cleanup
      if: always()
      run: docker rm -f sql-server